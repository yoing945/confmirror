# example/confmirror.yaml
settings:
  # name: "web-server"  # 可选，默认为当前目录名
  backup_root: "./mirror"
  script_hooks_dir: "./hooks"
  log_dir: "./logs"   # 日志目录，若未指定文件名则使用 settings.name
  git_auto_commit: true
  git_auto_push: false

modules:
  - name: "sshd"
    paths:
      - "/etc/ssh/sshd_config"
# Git配置备份工具 - 依赖包列表
# 此工具仅使用Python标准库，无需额外安装依赖包
# 如需扩展功能，可在此添加依赖包

# 可选依赖（用于增强功能）：
# python-dotenv>=0.19.0    # 更好的.env文件支持
# pyyaml>=6.0             # YAML配置文件支持
# click>=8.0              # 更强大的命令行界面
# rich>=12.0              # 美化终端输出
# tqdm>=4.60              # 进度条显示